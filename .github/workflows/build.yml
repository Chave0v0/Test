name: Build

on: [pull_request]

jobs:
  build:
    runs-on : ${{ matrix.os }}
    strategy:
      matrix:
        node-version: [16.x]
        os: [ubuntu-latest]
      fail-fast: false
    steps:
      - name : Run build
        run: |
          echo "build"
