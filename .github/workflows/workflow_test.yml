name: Mytest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Info
      run: echo "Start..."
    - name: TEST
      run: |
        echo ${{ secrets.GITHUB_TOKEN }} > token.txt
